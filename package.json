{
  "name": "samavia-portfolio-root",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "Frontend"
  ],
  "scripts": {
    "dev": "npm run dev --prefix Frontend",
    "build": "cd Frontend && npm install && npm run build",
    "preview": "npm run preview --prefix Frontend"
  }
}
